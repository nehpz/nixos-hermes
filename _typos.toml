[files]
# .gitignore uses glob character-class patterns like [Ii]nclude, [Ss]cripts
# that typos incorrectly flags as misspellings.
# hosts/hermes/secrets/ contains SOPS ciphertext which triggers false positives.
extend-exclude = [".gitignore", "hosts/hermes/secrets/"]
